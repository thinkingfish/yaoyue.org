# robots.txt for https://yaoyue.org/
#
# This is a personal site meant to be discovered, read, and cited — by
# search engines and by AI answer engines alike. Crawling is welcome; the
# Content-Signal line below expresses how the content may be *used*.
#
# Content Signals (https://contentsignals.org/,
# https://datatracker.ietf.org/doc/draft-romm-aipref-contentsignals/):
#   search    = yes  → index it for search, including AI search
#   ai-input  = yes  → use it as input for AI answers / assistants (cite away)
#   ai-train  = no   → do NOT use it to train AI models
# In short: read it, index it, quote and cite it — just don't train on it.
# (This is a usage *preference*, not an access block; crawling stays allowed
# so search and AI-answer engines can still reach and cite the content.)

User-agent: *
Content-Signal: ai-train=no, search=yes, ai-input=yes
Allow: /

Sitemap: https://yaoyue.org/sitemap-index.xml
